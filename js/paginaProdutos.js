// JAVASCRIPT
$(document).ready(function (keyframes, options) {

    let valorTotal = parseFloat("0").toLocaleString("pt-BR", {
        style: "currency",
        currency: "BRL",
        minimumFractionDigits: 2
    });

    $("#modeloPrancha").on('change', function() {

    });

    $("#btnCarrinho").text("Adicione ao carrinho: $euro;")

});